global class resetProfileScheduled implements Schedulable{
    global void execute (SchedulableContext sc){
        resetProfile();
    }
    
    public void resetProfile(){
   //     for(User u: lstUser){
     //       u.ProfileId = u.Previous_Profile__c;
       // }
        
        //update lstUser;
    }
}